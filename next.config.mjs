/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "lh3.googleusercontent.com",
      "openweathermap.org",
      "btibd.com",
      "res.cloudinary.com",
    ],
  },
};

export default nextConfig;
